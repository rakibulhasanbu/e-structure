export enum tagTypes {
  user = "user",
  comment = "comment",
  notification = "notification",
}

export const tagTypesList = Object.values(tagTypes);

export const METHOD = {
  POST: "POST",
  GET: "GET",
  PUT: "PUT",
  DELETE: "DELETE",
};
